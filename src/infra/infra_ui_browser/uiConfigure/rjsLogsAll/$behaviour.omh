;  called from a containing form
Calculate icMode as pcMode

If irefContainer     ;; should be rjsMenuApp
	Do irefContainer.$behaviour(icMode)
End If

Calculate $cinst.$objs.TabControl_Logs.$enabled as icMode=''