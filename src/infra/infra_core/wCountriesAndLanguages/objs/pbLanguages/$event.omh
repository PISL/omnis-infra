On evClick     ;; Event Parameters - pRow( Itemreference )
	OK message  {There is no wLanguages in this library}
	Quit method
	
	Test for window open {wLanguages}
	If flag true
		Bring window instance to front wLanguages
	Else
		Open window instance wLanguages/CEN (tSession,$ctask.tStatement)
	End If