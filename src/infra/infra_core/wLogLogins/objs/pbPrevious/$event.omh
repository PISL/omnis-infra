On evClick     ;; Event Parameters - pRow( Itemreference )
	Do method determineInterval
	Do method determineFromTo (-1)
	
	Do $cinst.$refreshUsers()
	Do $cinst.$refresh()
	
	Do $cinst.$behaviour()
	Do $cinst.$redraw()